import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Trakt - Integrations",
};

export default function IntegrationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
